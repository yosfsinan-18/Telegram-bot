name: Run Telegram Bot

on:
  schedule:
    - cron: '0 */5 * * *' # دەستپێکرنا ب ئۆتۆماتیکی پاش هەر 5 سەعتان
  workflow_dispatch: # ڕێدان ب دەستپێکرنا ب دەستی

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/latest-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Bot
        run: python bot.py
